WORDPRESS_API_URL=https://breathemon2dev.wpengine.com/graphql
# NEXT_PUBLIC_MAPBOX_TOKEN is set via GitHub Secrets (CI) / Vercel env vars (deploys).
# For local dev, add it to your .env.local:  NEXT_PUBLIC_MAPBOX_TOKEN=pk....
# Only required if you want to enable preview mode
# WORDPRESS_AUTH_REFRESH_TOKEN=
# WORDPRESS_PREVIEW_SECRET=
